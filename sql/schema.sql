CREATE TABLE used_cars (
    brand TEXT NOT NULL,
    model TEXT NOT NULL,
    year INTEGER NOT NULL,
    price REAL NOT NULL,
    transmission TEXT NOT NULL,
    mileage REAL NOT NULL,
    fuelType TEXT NOT NULL,
    tax REAL,
    mpg REAL,
    engineSize REAL,
    vehicle_age INTEGER,
    miles_per_year REAL,
    price_per_10k_miles REAL
);
